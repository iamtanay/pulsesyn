// Package sdk is the PulseSyn developer SDK — a thin, typed wrapper over the
// JSON-RPC API. It communicates with a PulseSyn node via HTTP client only and
// does not import any other protocol package directly.
package sdk
